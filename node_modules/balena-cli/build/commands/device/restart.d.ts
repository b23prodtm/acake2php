import { flags } from '@oclif/command';
import type { IArg } from '@oclif/parser/lib/args';
import Command from '../../command';
import type { BalenaSDK } from 'balena-sdk';
interface FlagsDef {
    help: void;
    service?: string;
}
export default class DeviceRestartCmd extends Command {
    static description: string;
    static examples: string[];
    static args: Array<IArg<any>>;
    static usage: string;
    static flags: flags.Input<FlagsDef>;
    static authenticated: boolean;
    run(): Promise<void>;
    restartServices(balena: BalenaSDK, deviceId: number | string, serviceNames: string[]): Promise<void>;
    restartAllServices(balena: BalenaSDK, deviceId: number | string): Promise<void>;
}
export {};
