import { flags } from '@oclif/command';
import type { IArg } from '@oclif/parser/lib/args';
import type { BalenaSDK } from 'balena-sdk';
import Command from '../../command';
interface ExtendedDevice extends DeviceWithDeviceType {
    application_name?: string;
}
interface FlagsDef {
    application?: string;
    app?: string;
    help: void;
}
export default class DeviceMoveCmd extends Command {
    static description: string;
    static examples: string[];
    static args: Array<IArg<any>>;
    static usage: string;
    static flags: flags.Input<FlagsDef>;
    static authenticated: boolean;
    run(): Promise<void>;
    interactivelySelectApplication(balena: BalenaSDK, devices: ExtendedDevice[]): Promise<ApplicationWithDeviceType>;
}
export {};
