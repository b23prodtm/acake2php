import { flags } from '@oclif/command';
import Command from '../../command';
interface FlagsDef {
    application?: string;
    app?: string;
    yes: boolean;
    advanced: boolean;
    'os-version'?: string;
    drive?: string;
    config?: string;
    help: void;
}
export default class DeviceInitCmd extends Command {
    static description: string;
    static examples: string[];
    static usage: string;
    static flags: flags.Input<FlagsDef>;
    static authenticated: boolean;
    run(): Promise<string>;
    configureOsImage(path: string, uuid: string, options: FlagsDef): Promise<void>;
    writeOsImage(path: string, deviceType: string, options: FlagsDef): Promise<void>;
}
export {};
