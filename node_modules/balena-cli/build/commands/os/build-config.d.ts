import { flags } from '@oclif/command';
import Command from '../../command';
import type { DeviceTypeJson } from 'balena-sdk';
interface FlagsDef {
    advanced: boolean;
    output: string;
    help: void;
}
export default class OsBuildConfigCmd extends Command {
    static description: string;
    static examples: string[];
    static args: {
        name: string;
        description: string;
        required: boolean;
    }[];
    static usage: string;
    static flags: flags.Input<FlagsDef>;
    static authenticated: boolean;
    run(): Promise<void>;
    buildConfig(image: string, deviceTypeSlug: string, advanced: boolean): Promise<any>;
    buildConfigForDeviceType(deviceTypeManifest: DeviceTypeJson.DeviceType, advanced: boolean): Promise<any>;
}
export {};
