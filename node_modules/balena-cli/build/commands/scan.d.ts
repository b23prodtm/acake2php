import { flags } from '@oclif/command';
import Command from '../command';
interface FlagsDef {
    json?: boolean;
    verbose: boolean;
    timeout?: number;
    help: void;
}
export default class ScanCmd extends Command {
    static description: string;
    static examples: string[];
    static usage: string;
    static flags: flags.Input<FlagsDef>;
    static primary: boolean;
    static root: boolean;
    run(): Promise<void>;
    protected static dockerInfoProperties: string[];
    protected static dockerVersionProperties: string[];
    protected static noDevicesFoundMessage: string;
    protected static windowsTipMessage: string;
}
export {};
