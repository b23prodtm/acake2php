import { flags } from '@oclif/command';
import Command from '../../command';
interface FlagsDef {
    help: void;
}
export default class AppRenameCmd extends Command {
    static description: string;
    static examples: string[];
    static args: ({
        name: string;
        description: string;
        required: boolean;
        parse: typeof import("../../utils/normalization").lowercaseIfSlug;
    } | {
        name: string;
        description: string;
    })[];
    static usage: string;
    static flags: flags.Input<FlagsDef>;
    static authenticated: boolean;
    run(): Promise<void>;
}
export {};
