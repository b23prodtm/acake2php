import { flags } from '@oclif/command';
import Command from '../../command';
declare type IArg<T> = import('@oclif/parser').args.IArg<T>;
interface FlagsDef {
    config: boolean;
    device: boolean;
    service: boolean;
    yes: boolean;
}
export default class EnvRmCmd extends Command {
    static description: string;
    static examples: string[];
    static args: Array<IArg<any>>;
    static usage: string;
    static flags: flags.Input<FlagsDef>;
    run(): Promise<void>;
}
export {};
