import { flags } from '@oclif/command';
import Command from '../command';
interface FlagsDef {
    application?: string;
    device?: string;
    release?: string;
    help: void;
    app?: string;
}
export default class TagsCmd extends Command {
    static description: string;
    static examples: string[];
    static usage: string;
    static flags: flags.Input<FlagsDef>;
    static authenticated: boolean;
    run(): Promise<void>;
    protected missingResourceMessage: string;
}
export {};
