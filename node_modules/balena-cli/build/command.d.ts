import Command from '@oclif/command';
export default abstract class BalenaCommand extends Command {
    static primary: boolean;
    static root: boolean;
    static authenticated: boolean;
    static readStdin: boolean;
    stdin: string;
    protected static checkElevatedPrivileges(): Promise<void>;
    static checkLoggedIn(): Promise<void>;
    static checkLoggedInIf(doCheck: boolean): Promise<void>;
    protected getStdin(): Promise<void>;
    protected static getLogger(): Promise<import("./utils/logger")>;
    protected init(): Promise<void>;
}
