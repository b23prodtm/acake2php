export function getDocker(options: {
    ca?: string;
    cert?: string;
    key?: string;
    docker?: string;
    dockerHost?: string;
    dockerPort?: number;
    host?: string;
    port?: number;
    timeout?: number;
}): Promise<import('docker-toolbelt')>;
export function createClient(opts: {
    host: string;
    port: number;
    timeout?: number;
    socketPath?: string;
}): import('docker-toolbelt');
