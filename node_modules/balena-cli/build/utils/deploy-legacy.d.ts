export function deployLegacy(docker: import('docker-toolbelt'), logger: import('./logger'), token: string, username: string, url: string, opts: {
    appName: string;
    imageName: string;
    buildLogs: string;
    shouldUploadLogs: boolean;
}): Promise<number>;
