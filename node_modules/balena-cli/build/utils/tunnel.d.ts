import type { BalenaSDK } from 'balena-sdk';
import { Socket } from 'net';
export declare const tunnelConnectionToDevice: (uuid: string, port: number, sdk: BalenaSDK) => Promise<(client: Socket) => Promise<void>>;
