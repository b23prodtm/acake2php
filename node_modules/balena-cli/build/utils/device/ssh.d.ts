export interface DeviceSSHOpts {
    address: string;
    port?: number;
    forceTTY?: boolean;
    verbose: boolean;
    service?: string;
}
export declare const deviceContainerEngineBinary = "$(if [ -f /usr/bin/balena ]; then echo \"balena\"; else echo \"docker\"; fi)";
export declare function performLocalDeviceSSH(opts: DeviceSSHOpts): Promise<void>;
