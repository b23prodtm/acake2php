import { Composition, ImageDescriptor } from './types';
export declare function defaultComposition(image?: string, dockerfile?: string): string;
export declare function normalize(o: any): Composition;
export declare function parse(c: Composition): ImageDescriptor[];
