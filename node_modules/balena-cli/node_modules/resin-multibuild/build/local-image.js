"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.LocalImage = void 0;
const errors_1 = require("./errors");
class LocalImage {
    constructor(daemon, name, serviceName, info) {
        this.daemon = daemon;
        this.external = info.external;
        this.successful = info.successful;
        this.serviceName = serviceName;
        if (name != null) {
            this.name = name;
        }
    }
    getImage() {
        if (this.name == null) {
            throw new Error('Attempting to get image without name');
        }
        return this.daemon.getImage(this.name);
    }
    deleteImage() {
        const image = this.getImage();
        return image.remove().catch(e => {
            throw new errors_1.ImageRemovalError(e);
        });
    }
}
exports.LocalImage = LocalImage;
//# sourceMappingURL=local-image.js.map