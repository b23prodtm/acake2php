export { RegistrySecretValidationError } from './errors';
export interface RegistrySecrets {
    [registryAddress: string]: {
        username: string;
        password: string;
    };
}
export declare class RegistrySecretValidator {
    private registrySecretJsonSchema;
    private validator;
    private validateFunction;
    validateRegistrySecrets(parsedJson: unknown): RegistrySecrets;
    parseRegistrySecrets(json: string): RegistrySecrets;
}
export declare function addCanonicalDockerHubEntry(registryconfig: RegistrySecrets): void;
export declare function getAuthConfigObj(imageName: string, registryconfig: RegistrySecrets): Promise<RegistrySecrets | {}>;
