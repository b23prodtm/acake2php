import * as t from 'io-ts';
declare type AcceptedVarList = VarList | string[];
export interface VarList {
    [key: string]: string;
}
export declare const PermissiveVarList: t.Type<VarList, AcceptedVarList, unknown>;
export {};
