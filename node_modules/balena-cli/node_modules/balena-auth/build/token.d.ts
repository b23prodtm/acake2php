/**
 * @module token
 */
export interface Token {
    type: TokenType;
    key: string;
    getAge: () => number | undefined;
    isExpired: () => boolean;
    isValid: () => boolean;
    needs2FA: () => boolean;
}
export declare enum TokenType {
    JWT = 0,
    APIKey = 1
}
