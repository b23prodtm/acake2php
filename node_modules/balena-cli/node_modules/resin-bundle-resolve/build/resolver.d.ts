import Bundle from './bundle';
import { FileInfo } from './fileInfo';
import { ParsedPathPlus } from './utils';
export { Bundle, FileInfo };
export interface Resolver {
    priority: number;
    name: string;
    dockerfileContents: string;
    entry(file: FileInfo): void;
    needsEntry(entryPath: ParsedPathPlus, specifiedDockerfilePath?: string): boolean;
    isSatisfied(bundle: Bundle): boolean;
    resolve(bundle: Bundle, specifiedDockerfilePath?: string, additionalTemplateVars?: Dictionary<string>): Promise<FileInfo>;
    getCanonicalName(specifiedPath: string): string;
}
