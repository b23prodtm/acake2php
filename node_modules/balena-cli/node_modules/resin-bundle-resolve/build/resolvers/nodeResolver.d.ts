import { Bundle, FileInfo, Resolver } from '../resolver';
import { ParsedPathPlus } from '../utils';
export declare class NodeResolver implements Resolver {
    priority: number;
    name: string;
    dockerfileContents: string;
    private packageJsonContent?;
    private hasScripts;
    entry(file: FileInfo): void;
    needsEntry(entryPath: ParsedPathPlus, specifiedDockerfilePath?: string): boolean;
    isSatisfied(_bundle: Bundle): boolean;
    resolve(bundle: Bundle, _specifiedDockerfilePath?: string): Promise<FileInfo>;
    getCanonicalName(_specifiedPath: string): string;
}
export default NodeResolver;
