import type * as LruCache from 'lru-cache';
import { PinejsClientCore, Params, AnyObject } from 'pinejs-client-core';
import { TypedError } from 'typed-error';
export declare class StatusError extends TypedError {
    statusCode: number;
    constructor(message: string, statusCode: number);
}
interface CachedResponse {
    etag: string;
    body: any;
}
export interface BackendParams {
    cache?: LruCache.Options<string, CachedResponse>;
}
export declare class PinejsClientRequest extends PinejsClientCore<PinejsClientRequest> {
    backendParams: BackendParams;
    private cache?;
    constructor(params: Params, backendParams?: BackendParams);
    _request(params: {
        method: string;
        url: string;
        body?: AnyObject;
    } & AnyObject): Promise<{}>;
}
export {};
