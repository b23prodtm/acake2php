import { Disposer } from 'bluebird';
export interface TmpFileResult {
    path: string;
    fd?: number;
}
export declare const tmpFile: (keepOpen?: boolean) => Promise<TmpFileResult>;
export declare const tmpFileDisposer: (keepOpen?: boolean) => Disposer<TmpFileResult>;
