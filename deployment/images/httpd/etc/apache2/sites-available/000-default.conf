# USAGE NOTICE:
# -------------
# this file will be filtered by program:
# and it cannot be installed "as is"
# Package gettext, command line: 
# envsubst < 'source-file' > 'destination-file'
# --------
# This Vhosts captures all requests that match www.* or ${SERVER_NAME}
Listen ${HTTP_PORT}
<VirtualHost _default_:${HTTP_PORT}>
    ServerName ${SERVER_NAME}
    ServerAlias www.${SERVER_NAME}
    ServerAdmin webmaster@${SERVER_NAME}
    ServerRoot ${SERVER_ROOT}
    DocumentRoot ${HTDOCS}
    ErrorLog "logs/${SERVER_NAME}-error_log"
    CustomLog "logs/${SERVER_NAME}-access_log" common
    DocumentRoot ${HTDOCS}
    <Directory "${HTDOCS}">
        DirectoryIndex index.php
        Options +FollowSymLinks
        AllowOverride FileInfo
        Require all granted
        FallbackResource /index.php
    </Directory>
    ProxyPassMatch ^/info$ fcgi://php-fpm:9001/info.php
    ProxyPassMatch ^/(.*\.php(/.*)?)$ fcgi://php-fpm:9001/$1
</VirtualHost>
